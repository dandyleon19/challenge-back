const successMessage = {
  success: true,
  message: "Operación exitosa."
}

const errorMessage = {
    success: false,
    message: "Ocurrió algo mal, intenta de nuevo."
}

module.exports = {
  successMessage,
  errorMessage
}
